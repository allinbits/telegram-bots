volumes:
  bounties:
  channels:

services:
  bountybot:
    image: ghcr.io/allinbits/telegram-bots/bountybot:dev
    build:
      context: .
      target: bountybot
    restart: unless-stopped
    tty: true
    environment:
      RPC_ENDPOINT: https://atomone-rpc.allinbits.com/
      MNEMONIC: 
      OWNERS: jaekwon777,albttx,clockworkgr
      # TG_TOKEN: ""
      DATABASE_PATH: /data/bounties.db
    # volumes:
    #   - bounties:/data

  channelbot:
    image: ghcr.io/allinbits/telegram-bots/channelbot:dev
    build:
      context: .
      target: channelbot
    restart: unless-stopped
    tty: true
    environment:
      OWNERS: jaekwon777,albttx,clockworkgr
      # TG_TOKEN: ""
      DATABASE_PATH: /data/channels.db
    # volumes:
    #   - channels:/data
